export class Quote {
    public quote: string;
    public character: string;

    constructor(quote: string, character: string){
        this.quote = quote;
        this.character = character;
    }
}